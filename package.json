{
  "name": "event-ticketing-system",
  "version": "1.0.0",
  "description": "Production-Ready Event Ticketing System",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../shared && npm install && cd ../frontend && npm install",
    "dev": "echo 'Silakan jalankan di terminal terpisah:' && echo '' && echo 'Terminal 1 (Backend): cd backend && npm run start:dev' && echo 'Terminal 2 (Frontend): cd frontend && npm run dev'",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "lint": "cd backend && npm run lint && cd ../frontend && npm run lint",
    "test": "cd backend && npm run test",
    "test:e2e": "cd backend && npm run test:e2e",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "setup:env": "cp backend/.env.example backend/.env && cp frontend/.env.example frontend/.env",
    "db:migrate": "npx prisma migrate dev --schema prisma/schema.prisma",
    "db:push": "npx prisma db push --schema prisma/schema.prisma",
    "db:generate": "npx prisma generate --schema prisma/schema.prisma",
    "db:seed": "npx tsx prisma/seed.ts",
    "db:reset": "npx prisma migrate reset --schema prisma/schema.prisma --force",
    "prisma:pull": "npx prisma db pull --schema prisma/schema.prisma",
    "prisma:generate": "npx prisma generate --schema prisma/schema.prisma",
    "status": "echo '✅ Project Setup Complete!' && echo '' && echo 'Backend: cd backend && npm run start:dev' && echo 'Frontend: cd frontend && npm run dev' && echo '' && echo 'API Docs: http://localhost:3000/api/docs' && echo 'RBAC Docs: http://localhost:3000/swagger' && echo 'Frontend: http://localhost:3001'"
  },
  "prisma": {
    "seed": "npx tsx prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "concurrently": "^8.2.2",
    "prisma": "^5.22.0",
    "tsx": "^4.23.1",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.17.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "clsx": "^2.1.1",
    "framer-motion": "^12.34.0",
    "tailwind-merge": "^3.4.0"
  }
}
