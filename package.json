{
  "name": "event-ticketing-system",
  "version": "1.0.0",
  "description": "Production-Ready Event Ticketing System",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../shared && npm install && cd ../frontend && npm install",
    "dev": "echo 'Silakan jalankan di terminal terpisah:' && echo '' && echo 'Terminal 1 (Backend): cd backend && npm run start:dev' && echo 'Terminal 2 (Frontend): cd frontend && npm run dev'",
    "dev:backend": "cd backend && npm run start:dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd backend && npm run build && cd ../frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "lint": "cd backend && npm run lint && cd ../frontend && npm run lint",
    "test": "cd backend && npm run test",
    "test:e2e": "cd backend && npm run test:e2e",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "setup:env": "cp backend/.env.example backend/.env && cp frontend/.env.example frontend/.env",
    "db:migrate": "cd backend && npx prisma migrate dev",
    "db:generate": "cd backend && npx prisma generate",
    "db:seed": "cd backend && npx prisma db seed",
    "status": "echo '✅ Project Setup Complete!' && echo '' && echo 'Backend: cd backend && npm run start:dev' && echo 'Frontend: cd frontend && npm run dev' && echo '' && echo 'API Docs: http://localhost:3000/api/docs' && echo 'Frontend: http://localhost:3000'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.17.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "framer-motion": "^12.34.0",
    "tailwind-merge": "^3.4.0"
  }
}
